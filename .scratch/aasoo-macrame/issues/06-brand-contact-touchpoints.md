# Brand contact touchpoints

Type: grilling
Status: resolved

## Answer

Contact touchpoints locked:

- **Channels**: Instagram + Telegram (both, per Q23), linked in the header/footer and a contact page.
- **Handles (placeholders — user corrects before build)**: Instagram `instagram.com/aasoo_macrame`, Telegram `t.me/aasoomacrame`.
- **Custom order request**: via the global contact link to the Telegram channel (Q18) — no quote form, no per-product contact.
- **Contact page**: maker info, both handles, note that order follow-up happens in the Telegram chat (where order confirmations land per 04).


## Question

Lock how customers reach the maker: Instagram + Telegram handles (Q23) placed in the header/footer and a contact page; a global "contact / custom request" link (glossary #3) — no per-product quote form (Q18). Confirm the exact handles/URLs to use, placement, and what the contact page contains (hours, order-follow-up via Telegram chat where order confirmation happens).

## Notes

- Custom orders bypass a form — a simple contact-to-message flow (Q18).
- Orders are reconciled in the same Telegram chat (glossary #2), so the contact page should reflect that.
